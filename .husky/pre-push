#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-push checks..."

# Type check (focused on core biomechanics code)
echo "⚡ TypeScript type checking (biomechanics & testing)..."
tsc --noEmit --project tsconfig.prepush.json || exit 1

# Run tests (focused on biomechanics & testing modules)
echo "🧪 Running biomechanics tests..."
npm test -- src/services/biomechanics src/testing src/utils/vectorMath --passWithNoTests || exit 1

echo "✅ Pre-push checks passed!"
