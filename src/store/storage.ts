/**
 * Persisted-state storage for iOS/Android: encrypted (Keychain / EncryptedSharedPreferences).
 * storage.web.ts is used by the web build instead.
 */
import EncryptedStorage from 'react-native-encrypted-storage';

export default EncryptedStorage;
